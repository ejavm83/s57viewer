# S-57 전자해도 웹 뷰어 - 상세 설명서

---

## 1. 프로젝트 개요

### 이 프로젝트는 무엇인가요?

이 프로젝트는 **대한민국 전자해도(Electronic Navigational Chart, ENC) 데이터를 웹 브라우저에서 볼 수 있는 지도 뷰어**입니다.

전자해도란 종이 해도(바다 지도)를 디지털화한 것으로, 선박이 안전하게 항해하기 위해 필요한 모든 정보를 담고 있습니다. 여기에는 바다의 깊이, 등대 위치, 부표(바다 위에 떠 있는 표지), 암초, 침몰 선박, 해안선 등의 정보가 포함됩니다.

### S-57이란?

**S-57**은 국제수로기구(IHO)가 정한 전자해도의 국제 표준 데이터 형식입니다. 전 세계 대부분의 해양 국가가 이 형식으로 전자해도를 제작하며, 선박에 탑재되는 ECDIS(전자해도표시시스템)에서 사용됩니다.

S-57 파일은 일반적인 이미지 파일이 아니라 **바이너리 형식의 구조화된 데이터**로, 특수한 소프트웨어 없이는 내용을 볼 수 없습니다. 이 프로젝트는 이러한 S-57 데이터를 일반 웹 브라우저에서 시각적으로 볼 수 있게 변환하여 보여줍니다.

### 사용되는 데이터

- **데이터 출처**: 대한민국 국립해양조사원 발행 전자해도
- **데이터 기준일**: 2024년 6월 21일 (NTM-25)
- **파일 수**: 744개의 해도 파일 (`.000` 확장자)
- **데이터 경로**: `C:\Users\kimkilyong\Documents\S57_KR전자해도(20240621)`
- **커버리지**: 대한민국 전 해역 (제주도, 독도, 이어도 포함)

> **주의**: 이 뷰어는 교육 및 시연 목적으로만 사용해야 하며, 실제 항해에 사용할 수 없습니다.

---

## 2. 시스템 구성

이 프로젝트는 크게 **백엔드(서버)**와 **프론트엔드(화면)** 두 부분으로 나뉩니다.

```
┌─────────────────────────────────────────────────────────┐
│  웹 브라우저 (프론트엔드)                                  │
│  ┌──────────┐  ┌───────────────────────────────────────┐ │
│  │ 사이드바    │  │          지도 (OpenLayers)             │ │
│  │ - 표시옵션  │  │  해안선, 수심, 등대, 부표, 침선 등      │ │
│  │ - 범례     │  │  ← 마우스로 이동/확대/축소              │ │
│  │ - 차트정보  │  │  ← 클릭하면 상세정보 팝업              │ │
│  └──────────┘  └───────────────────────────────────────┘ │
└────────────────────────┬────────────────────────────────┘
                         │ HTTP 요청 (자동)
                         ▼
┌─────────────────────────────────────────────────────────┐
│  Python 서버 (백엔드, FastAPI)                            │
│  1. S-57 파일 읽기 (pyogrio/GDAL)                        │
│  2. 지도 데이터로 변환 (GeoJSON)                           │
│  3. 웹 브라우저에 전달                                     │
│  4. 변환 결과 캐시 (재사용)                                │
└────────────────────────┬────────────────────────────────┘
                         │ 파일 읽기
                         ▼
┌─────────────────────────────────────────────────────────┐
│  S-57 전자해도 파일 (744개)                                │
│  KR1A0000.000, KR2F2000.000, KR5F4G13.000 ...           │
└─────────────────────────────────────────────────────────┘
```

### 파일 구조

```
D:\src\s-57 viewer\
├── server.py              ← 백엔드 서버 (데이터 처리 + API)
├── static\                ← 프론트엔드 파일들
│   ├── index.html         ← 화면 레이아웃 (HTML)
│   ├── style.css          ← 화면 디자인 (CSS)
│   └── app.js             ← 지도 동작 로직 (JavaScript)
├── cache\                 ← 변환된 데이터 캐시 (자동 생성)
│   ├── chart_index.json   ← 해도 목록 인덱스
│   └── *.json             ← 각 레이어별 변환 캐시
└── .claude\
    └── launch.json        ← 개발 서버 설정
```

---

## 3. 백엔드 서버 상세 설명 (`server.py`)

### 3.1 서버의 역할

서버는 3가지 핵심 작업을 수행합니다:

1. **S-57 파일 인덱싱**: 서버 시작 시 744개 파일을 스캔하여 각 파일이 어느 해역을 커버하는지, 어떤 축척인지, 어떤 정보가 들어있는지를 목록으로 만듭니다.

2. **S-57 → GeoJSON 변환**: 브라우저가 요청하면 해당 해역의 S-57 파일을 읽어서 웹 지도에서 사용할 수 있는 GeoJSON 형식으로 변환합니다.

3. **웹 페이지 제공**: HTML, CSS, JavaScript 파일을 브라우저에 전달합니다.

### 3.2 사용 기술

| 기술 | 역할 | 설명 |
|------|------|------|
| **Python 3.14** | 프로그래밍 언어 | 서버 전체가 Python으로 작성됨 |
| **FastAPI** | 웹 프레임워크 | 빠르고 현대적인 Python 웹 서버 |
| **pyogrio** | S-57 파서 | GDAL 엔진을 내장하여 S-57 파일을 읽는 라이브러리 |
| **uvicorn** | ASGI 서버 | FastAPI 앱을 실행하는 웹 서버 |

### 3.3 해도 축척(Scale) 체계

한국 전자해도 파일명의 3번째 글자가 축척 등급을 나타냅니다:

| 등급 | 파일명 패턴 | 대략적 축척 | 용도 | 예시 |
|------|-------------|------------|------|------|
| 1 | KR**1**xxxxx | 1:3,500,000 | 대양 항해용 (먼 바다 전체) | KR1F0000.000 |
| 2 | KR**2**xxxxx | 1:700,000 | 연안 항해용 (해안 근처) | KR2F2000.000 |
| 3 | KR**3**xxxxx | 1:180,000 | 접근 항해용 (항구 접근) | KR3F4G00.000 |
| 4 | KR**4**xxxxx | 1:90,000 | 연안 상세 | KR4F4G10.000 |
| 5 | KR**5**xxxxx | 1:22,000 | 항만 진입용 | KR5F4G13.000 |
| 6 | KR**6**xxxxx | 1:12,000 | 항만 내부 상세 | KR6xxxx.000 |

**축척이란?** 지도에서 1cm가 실제 거리 몇 m에 해당하는지를 나타냅니다. 1:12,000은 지도 1cm = 실제 120m를 의미합니다. 숫자가 작을수록 더 상세한 지도입니다.

### 3.4 줌 레벨과 축척 매핑

사용자가 지도를 확대/축소하면, 서버는 현재 줌 레벨에 맞는 축척의 해도를 자동으로 선택합니다:

| 줌 레벨 | 화면 축척 (대략) | 로드하는 해도 등급 |
|---------|-----------------|-------------------|
| 4~5 | 1:25,000,000 ~ 1:50,000,000 | 등급 1, 2 |
| 6~7 | 1:5,000,000 ~ 1:10,000,000 | 등급 1, 2, 3 |
| 8~9 | 1:700,000 ~ 1:3,500,000 | 등급 2, 3, 4 |
| 10~11 | 1:180,000 ~ 1:350,000 | 등급 3, 4, 5 |
| 12~13 | 1:45,000 ~ 1:90,000 | 등급 4, 5, 6 |
| 14 이상 | 1:22,000 이하 | 등급 5, 6 |

### 3.5 API 엔드포인트

서버는 다음과 같은 API를 제공합니다:

| API 경로 | 설명 | 사용 시점 |
|----------|------|----------|
| `GET /api/index` | 전체 해도 목록과 각 해도의 범위 반환 | 페이지 로드 시 |
| `GET /api/charts` | 현재 화면 범위와 줌에 맞는 해도 데이터 반환 | 지도 이동/확대 시 자동 호출 |
| `GET /api/chart/{name}` | 특정 해도 하나의 전체 데이터 반환 | 개별 해도 조회 시 |
| `GET /api/categories` | 표시 카테고리 목록 반환 | 사이드바 구성 시 |

#### `/api/charts` 파라미터 상세

| 파라미터 | 설명 | 예시 |
|----------|------|------|
| `west` | 화면 좌측 경도 | 125.0 |
| `south` | 화면 하단 위도 | 33.0 |
| `east` | 화면 우측 경도 | 130.0 |
| `north` | 화면 상단 위도 | 38.0 |
| `zoom` | 현재 줌 레벨 | 8 |
| `layers` | 요청할 레이어 목록 (콤마 구분) | LNDARE,COALNE,LIGHTS |

### 3.6 데이터 변환 과정

S-57 파일이 브라우저에서 표시되기까지의 과정:

```
S-57 파일 (.000)
    │
    ▼ pyogrio로 읽기
바이너리 지오메트리 (WKB 형식)  +  속성 데이터 (테이블)
    │
    ▼ WKB 파싱 (parse_wkb 함수)
좌표 데이터 (경도, 위도)
    │
    ▼ GeoJSON 변환
{
  "type": "Feature",
  "geometry": {"type": "Point", "coordinates": [129.05, 35.10]},
  "properties": {"layer": "LIGHTS", "COLOUR": 1}
}
    │
    ▼ JSON 캐시 저장 (cache/ 폴더)
재사용 가능한 캐시 파일
    │
    ▼ HTTP 응답
브라우저의 OpenLayers가 지도에 렌더링
```

### 3.7 캐싱 시스템

서버는 두 단계의 캐시를 사용합니다:

1. **해도 인덱스 캐시** (`cache/chart_index.json`)
   - 744개 파일의 범위, 축척, 포함 레이어 정보
   - 최초 실행 시 1~2분 소요, 이후 즉시 로드

2. **레이어 데이터 캐시** (`cache/*.json`)
   - 각 해도 파일의 각 레이어별 GeoJSON 변환 결과
   - 한 번 변환하면 다음부터는 캐시에서 즉시 로드
   - 캐시 키는 파일 경로와 레이어명의 MD5 해시

> **캐시 초기화**: `cache/` 폴더를 삭제하면 모든 캐시가 초기화되며, 다음 실행 시 다시 생성됩니다.

---

## 4. 프론트엔드 상세 설명

### 4.1 화면 구성

화면은 크게 4개 영역으로 나뉩니다:

```
┌──────────────────────────────────────────────────┐
│  상단바 (S-57 Web Viewer | Dual Mode | 버튼들)     │
├──────────┬───────────────────────────────────────┤
│          │                                       │
│  사이드바  │           지도 영역                    │
│          │                                       │
│ -제품목록  │    해안선, 수심, 등대 등                │
│ -국가선택  │    렌더링된 해도                        │
│ -표시옵션  │                                       │
│ -차트정보  │                         ┌──────────┐ │
│ -범례     │                         │ 피처 팝업  │ │
│          │                         └──────────┘ │
├──────────┴───────────────────────────────────────┤
│  하단바 (축척 선택 | 면책조항 | 좌표 표시)            │
└──────────────────────────────────────────────────┘
```

### 4.2 사이드바 기능

#### Product List (제품 목록)
- **S-57 Electronic Charts**: 전자해도 표시를 켜고 끄는 토글 스위치입니다. 끄면 모든 해도 데이터가 지도에서 사라집니다.

#### Country (국가)
- 현재 **Republic of Korea (대한민국)**으로 고정되어 있습니다. 사용 중인 데이터가 한국 해도이기 때문입니다.

#### Display Option (표시 옵션)
각 해도 요소를 카테고리별로 켜고 끌 수 있습니다:

| 옵션 | 색상 | 포함하는 해도 요소 | 설명 |
|------|------|-------------------|------|
| **Full Display** | 주황 | 전체 | 모든 카테고리를 한번에 켜기/끄기 |
| **Land** | 갈색 | 육지, 랜드마크, 고도, 지역명, 호수 | 땅으로 된 모든 지형 |
| **Light** | 노랑 | 등대, 무신호(안개 신호) | 빛을 내는 항해 보조시설 |
| **Beacon** | 빨강 | 방위표지, 고립장애표지, 측방표지, 레이더 트랜스폰더 | 고정된 표지 구조물 |
| **Buoy** | 초록 | 고립위험부표, 측방부표, 안전수역부표, 특수부표 | 바다에 떠 있는 표지 |
| **Obstruction** | 보라 | 장애물, 수중 암초 | 항해에 위험한 물체 |
| **Wreck** | 분홍 | 침몰 선박 | 바다 밑에 가라앉은 배 |
| **Sounding** | 청록 | 수심 측량값 | 숫자로 표시되는 바다 깊이 |
| **Depth Area** | 파랑 | 수심 구역, 등심선, 해저 바닥 | 색으로 구분되는 수심 영역 |
| **Coastline** | 회색 | 해안선, 호안(해안 구조물) | 바다와 육지의 경계선 |
| **Navigation** | 주황 | 심수 항로, 양방향 항로, 여객선 항로, 정박지, 통제점 | 선박이 다니는 길 관련 |
| **Infrastructure** | 갈색 | 교량, 가공케이블, 해저케이블, 해저파이프라인, 계선시설, 댐, 부잔교, 교각 | 인공 구조물 |

#### Chart Info (해도 정보)
현재 화면에 로드된 해도 데이터의 요약 정보:
- **Charts loaded**: 현재 로드된 해도 파일 수 (최대 30개)
- **Features**: 현재 표시 중인 총 지형지물 개수
- **Zoom**: 현재 줌 레벨 (4~18)
- **Scale**: 현재 화면의 대략적 축척

#### Legend (범례)
지도에 표시되는 색상과 기호의 의미:

| 기호/색상 | 의미 |
|----------|------|
| 베이지/연갈색 면 | 육지 |
| 연한 하늘색 면 | 얕은 바다 (5m 미만) |
| 중간 파란색 면 | 보통 깊이 (5~20m) |
| 진한 파란색 면 | 깊은 바다 (20m 이상) |
| 노란 원 (후광 있음) | 등대 |
| 빨간/초록 다이아몬드 | 등표 (Beacon) |
| 빨간/초록/노란 삼각형 | 부표 (Buoy) |
| 분홍색 X 표시 | 침몰 선박 (Wreck) |
| 보라색 별 표시 | 장애물/암초 (Obstruction) |

### 4.3 지도 영역

#### 지도 조작 방법

| 조작 | 방법 |
|------|------|
| **이동** | 마우스 왼쪽 버튼을 누른 채 드래그 |
| **확대** | 마우스 휠 위로 또는 우측 상단 `+` 버튼 |
| **축소** | 마우스 휠 아래로 또는 우측 상단 `-` 버튼 |
| **상세정보 보기** | 해도 요소(등대, 부표 등)를 클릭 |
| **좌표 확인** | 마우스를 움직이면 우측 하단에 위도/경도 표시 |

#### 자동 데이터 로딩
- 지도를 이동하거나 확대/축소하면 **400ms 후 자동으로** 새로운 데이터를 서버에 요청합니다.
- 현재 화면에 보이는 영역과 줌 레벨에 맞는 해도만 선택적으로 로드합니다.
- 로딩 중에는 화면 중앙에 "Loading charts..." 표시가 나타납니다.

#### 피처 클릭 팝업
지도 위의 요소를 클릭하면 우측 상단에 상세 정보 팝업이 나타납니다. 표시되는 정보 예시:

| 속성 | 설명 | 예시 |
|------|------|------|
| layer | 레이어 종류 | LIGHTS (Light) |
| OBJNAM | 개체 이름 | 소흑산도등대 |
| COLOUR | 등대 빛 색상 코드 | 1 (흰색) |
| CATLIT | 등대 종류 | 1 (방향등) |
| HEIGHT | 높이 (미터) | 35 |
| VALNMR | 광달거리 (해리) | 15 |
| depth | 수심 (미터, 측심값) | 12.5 |

### 4.4 하단바

| 요소 | 설명 |
|------|------|
| **축척 선택** | 드롭다운에서 원하는 축척을 선택하면 해당 줌으로 이동 |
| **면책조항** | "이 지도는 항해 목적으로 사용할 수 없습니다" |
| **좌표 표시** | 마우스 커서 위치의 위도/경도를 실시간으로 표시 |

### 4.5 지도 렌더링 기술 (OpenLayers)

이 프로젝트는 **OpenLayers**라는 오픈소스 지도 라이브러리를 사용합니다. OpenLayers는 Google Maps처럼 웹에서 지도를 표시할 수 있는 JavaScript 라이브러리입니다.

#### 좌표계
- **데이터 좌표계**: EPSG:4326 (경도/위도, WGS84)
  - 일상적으로 사용하는 GPS 좌표와 같은 형식입니다.
- **지도 표시 좌표계**: EPSG:3857 (Web Mercator)
  - 웹 지도에서 표준으로 사용하는 좌표계입니다.
  - OpenLayers가 자동으로 변환합니다.

#### 레이어 렌더링 순서
지도 요소들은 아래에서 위로 다음 순서로 그려집니다 (아래 것이 먼저 그려져서 뒤에 위치):

```
1단계 (바닥): 미조사해역 → 해역 → 수심구역 → 호수 → 육지
2단계 (선):   등심선 → 해안선 → 호안
3단계 (구역): 정박지 → 항로 → 여객선 항로 → 케이블 → 파이프라인
4단계 (구조): 교량 → 댐 → 부잔교 → 계선시설
5단계 (위험): 장애물 → 수중암초 → 침선
6단계 (수심): 측심값 (숫자)
7단계 (표지): 부표 → 등표 → 두표 → 말뚝
8단계 (상단): 랜드마크 → 레이더 → 무선국 → 등대
```

#### 줌 레벨별 표시 필터링
모든 요소를 항상 표시하면 지도가 지저분해지므로, 줌 레벨에 따라 표시를 자동 조절합니다:

| 요소 | 표시 조건 |
|------|----------|
| 측심값 (숫자) | 줌이 충분히 확대된 경우에만 표시 |
| 등심선 | 중간 줌 이상에서만 표시 |
| 침선 기호 | 중간 줌 이상에서만 표시 |
| 장애물 기호 | 중간 줌 이상에서만 표시 |
| 랜드마크 | 중간 줌 이상에서만 표시 |
| 수심구역 경계선 | 줌이 작으면 얇게, 크면 두껍게 |
| 등대 후광 | 줌이 클수록 후광 크기 증가 |

### 4.6 수심 색상 체계

바다의 깊이에 따라 다른 색으로 표시됩니다:

| 수심 범위 | 색상 | 의미 |
|----------|------|------|
| 0m 이하 (간출지) | 연녹색 | 썰물 때 드러나는 곳 |
| 0 ~ 5m | 아주 연한 파랑 | 매우 얕음 (소형 선박 주의) |
| 5 ~ 10m | 연한 파랑 | 얕음 |
| 10 ~ 20m | 중간 파랑 | 보통 |
| 20 ~ 50m | 진한 파랑 | 깊음 |
| 50m 이상 | 매우 진한 파랑 | 매우 깊음 |

---

## 5. S-57 데이터 레이어 용어 사전

S-57 표준에서 사용하는 레이어 코드와 그 의미입니다:

### 지형 레이어

| 코드 | 영문명 | 한글명 | 설명 |
|------|--------|--------|------|
| LNDARE | Land Area | 육지 영역 | 땅으로 된 영역 (섬 포함) |
| LAKARE | Lake Area | 호수 | 내륙의 담수 호수 |
| SEAARE | Sea Area | 해역 | 바다 영역에 이름을 붙인 구역 |
| COALNE | Coastline | 해안선 | 육지와 바다의 경계선 |
| SLCONS | Shoreline Construction | 호안/해안 구조물 | 방파제, 제방 등 인공 해안 구조물 |
| LNDMRK | Landmark | 랜드마크 | 항해 시 위치 파악에 도움이 되는 육상 표지 (교회 첨탑, 굴뚝 등) |
| LNDELV | Land Elevation | 육상 고도 | 육지의 높이 정보 |
| LNDRGN | Land Region | 육상 지역명 | 육지 지역의 이름 |

### 수심 레이어

| 코드 | 영문명 | 한글명 | 설명 |
|------|--------|--------|------|
| DEPARE | Depth Area | 수심 구역 | 일정 수심 범위를 가진 바다 영역 (DRVAL1~DRVAL2) |
| DEPCNT | Depth Contour | 등심선 | 같은 수심을 이은 선 (VALDCO 값) |
| SOUNDG | Sounding | 측심값 | 특정 지점의 정확한 수심 (숫자로 표시) |
| SBDARE | Seabed Area | 해저면 | 해저 바닥의 종류 (모래, 진흙, 암반 등) |

### 항해 표지 레이어

| 코드 | 영문명 | 한글명 | 설명 |
|------|--------|--------|------|
| LIGHTS | Light | 등대/등화 | 밤에 빛을 내어 선박을 안내하는 시설 |
| FOGSIG | Fog Signal | 무신호 | 안개 속에서 소리로 위치를 알리는 장치 |
| BOYLAT | Buoy, Lateral | 측방부표 | 항로의 좌/우 경계를 표시하는 부표 (빨강=우현, 초록=좌현) |
| BOYISD | Buoy, Isolated Danger | 고립위험부표 | 주변은 안전하지만 그 위치에 위험물이 있음을 표시 |
| BOYSAW | Buoy, Safe Water | 안전수역부표 | 주변 수역이 안전함을 표시 (항로 중앙 등) |
| BOYSPP | Buoy, Special Purpose | 특수부표 | 특수 목적(군사구역, 양식장 등)을 표시 |
| BCNCAR | Beacon, Cardinal | 방위표지 | 위험물의 방향을 알려주는 고정 표지 |
| BCNISD | Beacon, Isolated Danger | 고립장애표지 | 고립된 위험물 위에 세운 고정 표지 |
| BCNLAT | Beacon, Lateral | 측방표지 | 항로 경계를 표시하는 고정 표지 |
| TOPMAR | Top Mark | 두표 | 부표나 등표 꼭대기에 있는 식별용 형상물 |
| RTPBCN | Radar Transponder Beacon | 레이더 응답기 | 선박 레이더에 응답하는 장치 |

### 위험물 레이어

| 코드 | 영문명 | 한글명 | 설명 |
|------|--------|--------|------|
| OBSTRN | Obstruction | 장애물 | 항해에 방해가 되는 수중/수면 물체 |
| UWTROC | Underwater Rock | 수중 암초 | 물 밑에 잠긴 바위 |
| WRECKS | Wreck | 침몰선 | 바다 밑에 가라앉은 선박 잔해 |

### 항해/교통 레이어

| 코드 | 영문명 | 한글명 | 설명 |
|------|--------|--------|------|
| DWRTPT | Deep Water Route Part | 심수 항로 | 대형 선박이 다니는 깊은 물길 |
| TWRTPT | Two-Way Route Part | 양방향 항로 | 양방향 통행이 가능한 항로 |
| FERYRT | Ferry Route | 여객선 항로 | 정기 여객선이 다니는 길 |
| ACHBRT | Anchorage | 정박지 | 선박이 닻을 내리고 정박하는 구역 |
| RDOCAL | Radio Calling-In Point | 무선 호출 지점 | 선박이 무선으로 보고해야 하는 지점 |
| RDOSTA | Radio Station | 무선국 | 해양 무선 통신 기지국 |
| CTRPNT | Control Point | 통제점 | 측량 기준점 |
| PILPNT | Pile | 말뚝 | 물속에 박은 기둥 |

### 시설물 레이어

| 코드 | 영문명 | 한글명 | 설명 |
|------|--------|--------|------|
| BRIDGE | Bridge | 교량 | 바다/강 위를 지나는 다리 |
| CBLOHD | Cable, Overhead | 가공 케이블 | 공중에 걸린 전선/케이블 (선박 마스트 주의) |
| CBLSUB | Cable, Submarine | 해저 케이블 | 바다 밑에 깔린 통신/전력 케이블 (투묘 금지) |
| PIPSOL | Pipeline, Submarine/On Land | 파이프라인 | 해저 또는 육상 파이프라인 |
| MORFAC | Mooring Facility | 계선시설 | 선박을 묶어두는 시설 (부이, 돌핀 등) |
| DAMCON | Dam | 댐 | 댐, 방조제 |
| PONTON | Pontoon | 부잔교 | 물에 뜨는 부두/다리 |
| PYLONS | Pylon | 교각/철탑 | 교량 기둥, 송전 철탑 |

### 메타 레이어

| 코드 | 영문명 | 한글명 | 설명 |
|------|--------|--------|------|
| M_COVR | Coverage | 커버리지 | 해도가 다루는 지리적 범위 |
| M_QUAL | Quality | 품질 정보 | 데이터의 정확도/신뢰도 |
| UNSARE | Unsurveyed Area | 미조사 해역 | 아직 측량하지 않은 바다 영역 |

---

## 6. 실행 방법

### 6.1 사전 요구사항

| 소프트웨어 | 최소 버전 | 설치 확인 명령 |
|-----------|----------|---------------|
| Python | 3.10 이상 | `python --version` |
| pip | 최신 | `pip --version` |

### 6.2 의존성 설치

```bash
pip install fastapi uvicorn pyogrio
```

| 패키지 | 용도 |
|--------|------|
| `fastapi` | 웹 서버 프레임워크 |
| `uvicorn` | FastAPI 실행 엔진 |
| `pyogrio` | S-57 파일 읽기 (GDAL 엔진 내장) |

### 6.3 서버 실행

```bash
cd "D:\src\s-57 viewer"
python server.py
```

서버가 시작되면 콘솔에 다음과 같이 표시됩니다:

```
Building chart index from S-57 files...    ← 최초 실행 시 (1~2분 소요)
  Indexed 50/744 files...
  Indexed 100/744 files...
  ...
Indexed 744 charts
INFO:     Uvicorn running on http://0.0.0.0:8080    ← 서버 준비 완료
```

두 번째 실행부터는 캐시를 사용하므로 즉시 시작됩니다:

```
Loaded chart index from cache: 744 charts    ← 캐시에서 즉시 로드
INFO:     Uvicorn running on http://0.0.0.0:8080
```

### 6.4 접속

웹 브라우저에서 다음 주소로 접속합니다:

```
http://localhost:8080
```

같은 네트워크의 다른 컴퓨터에서 접속하려면:

```
http://[서버컴퓨터IP]:8080
```

### 6.5 서버 종료

콘솔에서 `Ctrl + C`를 누르면 서버가 종료됩니다.

---

## 7. 성능 관련 참고사항

### 최초 실행 시
- 744개 S-57 파일 인덱싱에 약 1~2분 소요
- 인덱스 생성 후 `cache/chart_index.json`에 저장되어 재사용

### 데이터 로딩
- 각 해도의 각 레이어를 처음 요청하면 S-57 파싱 + GeoJSON 변환이 수행됨 (수초 소요)
- 변환 결과는 `cache/` 폴더에 JSON으로 저장되어 이후에는 즉시 응답
- 한 번에 최대 30개 해도까지 로드 (성능 보호)

### 메모리 사용
- 줌 레벨에 따라 수만 개의 피처가 브라우저에 로드될 수 있음
- 매우 많은 피처가 로드되면 브라우저가 느려질 수 있으므로, 사이드바에서 불필요한 레이어를 끄는 것이 좋음

### 캐시 관리
- 캐시 초기화가 필요하면 `cache/` 폴더 전체를 삭제
- 해도 데이터가 업데이트되면 캐시를 삭제하고 서버를 재시작해야 함

---

## 8. 문제 해결

| 증상 | 원인 | 해결 방법 |
|------|------|----------|
| 서버 시작 시 "ModuleNotFoundError" | 의존성 미설치 | `pip install fastapi uvicorn pyogrio` 실행 |
| 지도가 빈 화면으로 표시 | 데이터 로딩 중 | "Loading charts..." 표시가 사라질 때까지 대기 |
| 특정 해역에 데이터가 없음 | 해당 축척의 해도 부재 | 줌을 축소하여 더 넓은 범위의 해도 로드 |
| 브라우저가 느려짐 | 피처가 너무 많음 | 사이드바에서 불필요한 레이어 끄기 |
| "Port 8080 already in use" | 이전 서버가 아직 실행 중 | 이전 서버 프로세스 종료 후 재시작 |
| 캐시 오류 | 캐시 파일 손상 | `cache/` 폴더 삭제 후 재시작 |

---

## 9. 기술 아키텍처 요약

```
┌─────────────────────────────────────────────────┐
│                    클라이언트                      │
│                                                  │
│  HTML (index.html)    웹 페이지 구조               │
│  CSS  (style.css)     다크 테마 스타일링            │
│  JS   (app.js)        OpenLayers 10.5 기반         │
│                       - 벡터 레이어 렌더링           │
│                       - S-57 심볼로지 스타일링       │
│                       - 줌 기반 자동 데이터 로딩      │
│                       - 피처 팝업 표시              │
│                       - 카테고리별 표시/숨기기        │
└────────────────────────┬────────────────────────┘
                         │
                    REST API (JSON)
                         │
┌────────────────────────┴────────────────────────┐
│                     서버                          │
│                                                  │
│  Python 3.14 + FastAPI + Uvicorn                 │
│                                                  │
│  pyogrio (GDAL)  →  S-57 바이너리 파싱             │
│  WKB 파서        →  지오메트리 좌표 추출             │
│  GeoJSON 변환기  →  웹 호환 지도 데이터 생성          │
│  캐시 관리       →  중복 변환 방지                   │
│  공간 필터링     →  화면 범위 내 해도만 선택          │
│  축척 매핑       →  줌 레벨에 맞는 상세도 해도 선택    │
└────────────────────────┬────────────────────────┘
                         │
                    파일 시스템
                         │
┌────────────────────────┴────────────────────────┐
│              S-57 전자해도 데이터                   │
│                                                  │
│  744개 .000 파일 (ISO 8211 형식)                   │
│  축척 등급 1~6 (대양 ~ 항만 상세)                   │
│  대한민국 전 해역 커버리지                           │
│  2024년 6월 21일 기준                              │
└─────────────────────────────────────────────────┘
```
